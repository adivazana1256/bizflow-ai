services:
  db:
    image: postgres:16
    container_name: bizflow-db
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: bizflow
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
